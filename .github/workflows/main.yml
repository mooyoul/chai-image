name: workflow
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    container: node:lts
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v1
      - name: Prepare
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
